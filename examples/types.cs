class Test {
    int hex = 0x1A;
    long big = 123L;
    double real = 3.14e-2;
    float f = 2.5f;
    char ch = 'A';
    string s = "String";
    bool ok = true;
    object n = null;
    // операторы
    a = b + c - d * e / f % g;
    if (x > y && y <= z || !flag) { }
    i++; j--;
    x <<= 2;
}